export enum SLideEnum {
  CONTEST = "contest",
  TEAM = "team",
}

export enum BuyTokenSlideEnum {
  POL = "POL",
  USDT = "USDT",
}

export enum ContestSlideEnum {
  FEATURED = "Featured",
  USER_CONTEST = "User Contest",
}
